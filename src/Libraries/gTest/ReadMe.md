# Google Test with Google Mock library Version 1.7

Contains copy of gTest repo https://github.com/google/googletest

Additionally: /stage directory contains my VS files (original ones are not good enough). Use stage.zip for them.


```
\--1.7
   +--googlemock
   |  +--build-aux        // no change
   |  +--docs             // no change
   |  +--include          // no change
   |  +--make             // no change
   |  +--scripts          // no change
   |  +--src              // no change
   |  +--stage            //
   |  |  \--msvc14        // contains solution files (*.sln, *.vcxproj)
   |  |     +--Win32      //
   |  |     |  +--Debug   // Result of project rebuild
   |  |     |  \--Release // Result of project rebuild
   |  |     \--x64        //
   |  |        +--Debug   // Result of project rebuild
   |  |        \--Release // Result of project rebuild
   |  \--test             // no change
   \--googletest          // no change
      +--build-aux        // no change
      +--cmake            // no change
      +--codegear         // no change
      +--docs             // no change
      +--include          // no change
      +--m4               // no change
      +--make             // no change
      +--samples          // no change
      +--scripts          // no change
      +--src              // no change
      +--stage            //
      |  \--msvc14        // contains solution files (*.sln, *.vcxproj)
      |     +--Win32      //
      |     |  +--Debug   // Result of project rebuild
      |     |  \--Release // Result of project rebuild
      |     \--x64        //
      |        +--Debug   // Result of project rebuild
      |        \--Release // Result of project rebuild
      +--test             // no change
      \--xcode            // no change
```
